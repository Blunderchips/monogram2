import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.blunderchips.monogram2',
  appName: 'monogram2',
  webDir: 'dist/monogram2',
  bundledWebRuntime: false
};

export default config;
